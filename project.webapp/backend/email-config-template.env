# Email Configuration - Copy these lines to your .env file
# Replace the placeholder values with your actual Gmail credentials

# Gmail SMTP Configuration for sending real emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASS=your-16-character-app-password
SMTP_FROM=your-gmail-address@gmail.com

# Instructions:
# 1. Replace 'your-gmail-address@gmail.com' with your actual Gmail address
# 2. Replace 'your-16-character-app-password' with your Gmail App Password
# 3. Copy these lines to your .env file, replacing the existing email configuration
# 4. Make sure to enable 2-Factor Authentication on your Gmail account
# 5. Generate an App Password from Google Account settings 